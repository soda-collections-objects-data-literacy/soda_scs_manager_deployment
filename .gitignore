# IDE / editor
.vscode
.cursor

# Secrets and local environment
*.env
*.env.local
acme.json

# Data, backups, archives
volumes
backups/
**/*.tar.gz
exports

# Site-specific / generated
helpers
configure_man
openrefine
Openrefine
lontod/data/ontologies
ontology_resolver
webprotege/.protegedata

# scs-health (Next.js) — local build output and dependencies
# Source is tracked; see also scs-health/.gitignore
scs-health/node_modules/
scs-health/.next/
scs-health/out/
scs-health/*.tsbuildinfo
scs-health/.env*
scs-health/npm-debug.log*
scs-health/yarn-debug.log*
scs-health/yarn-error.log*
scs-health/.pnpm-debug.log*

roBot_rules